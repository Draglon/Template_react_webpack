import { getCookie, setCookie } from '../format';

describe('Helpers - formats', () => {
  it('getCookie() - should return string', () => {
    // const cookie = 'name';
    // expect(getCookie(cookie)).toEqual('100,000.00');
  });

  it('setCookie() - should return string', () => {
    // const cookie = 140;
    // expect(setCookie(cookie)).toEqual('2h 20m');
  });
});
